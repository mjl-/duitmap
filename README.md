duitmap - map

created as demo for duit

# todo

- attribution, OSM and search (licence from response)
- make it a separate ui

- should show errors to user, instead of using log
- prefetch on hover over + or -, and prefetch one border of tiles around visible map
- tile cache management. keep 25 images in memory. keep 100 tile buffers in memory, lru.
